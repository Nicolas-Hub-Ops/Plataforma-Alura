# APIs usando NodeJS, Express e MongoDB - Introdução
  
- > Use `npm install` para instalar as dependências do projeto
- > Em seguida use `npm run dev` para rodar a API
- > Porta será gerada em `http://localhost:3000`
